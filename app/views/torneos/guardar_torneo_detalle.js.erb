<% if @guardado_ok %>

 
setTimeout( function(){ 
  
    $("#<%= params[:window_id]%>").modal('hide');
    $("#link-to-torneo-detalle-<%= params[:torneo_id]%>").trigger('click');
    $("#link-to-torneo-detalle-<%= params[:torneo_id]%>").trigger('click');
    swal("La fecha se agregó al campeonato exitosamente!");

    }, 1000);
 
<% else %>

  swal('No se pudo guardar la Fecha del campeonato. Intente más tarde.');

<% end %>