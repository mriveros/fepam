<% if @guardado_ok %>

 
setTimeout( function(){ 
  
    $("#<%= params[:window_id]%>").modal('hide');
    $("#link-to-torneo-detalle-<%= params[:torneo_detalle_torneo_id]%>").trigger('click');
    $("#link-to-torneo-detalle-<%= params[:torneo_detalle_torneo_id]%>").trigger('click');
    swal("El Potrero se agregó a la Hacienda exitosamente!");

    }, 1000);
 
<% else %>

  swal('No se pudo guardar la Fecha del campeonato. Intente más tarde.');

<% end %>