<% if torneos.present? %>

  <% torneos.each do |dc| %>

    <tr>

      <td ><%= dc.id %></td>
      <td ><%= dc.descripcion%></td>
      <td ><%= dc.cantidad_fechas%></td>
      <td >
        
        <%= link_to_editar_torneo(dc) %>

        <%= link_to icon_eliminar, torneos_eliminar_path(id: dc.id), remote: true, class: "btn btn-danger btn-xs", title: 'Haga click para eliminar el Cargo', data: { confirm: "Está seguro de eliminar el Cargo?"} %>
         
       </td>

      </td>

    </tr>

  <% end %>

<tr id="tab-paginacion">

    <td colspan="12" style="text-align:center;"><div class="flickr_pagination"><%= ajax_will_paginate torneos, :params => params %></div></td>
</tr>

<tr>
    <td colspan="12">
      Cantidad de registros encontrados: <%= @total_encontrados %> 
    </td>
</tr>

 

<% else %>

  <tr>

    <td colspan="12"><p class='none-result'>No se han encontrado resultados.</p></td>

  </tr>


<% end %>
